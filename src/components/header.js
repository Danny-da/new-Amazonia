import React from 'react';
import './estilo.css';

const Header = () => {
  return (
    <header>
      
      <nav>
        {/* Agrega tus elementos de navegación aquí */}
      </nav>
    </header>
  );
};

export default Header;
